{
  "name": "treinador-banks",
  "version": "1.0.0",
  "scripts": {
    "start": "browser-sync start --server --files \"*.html, *.css, *.js\" --port 3000"
  },
  "devDependencies": {
    "browser-sync": "^3.0.2"
  }
}